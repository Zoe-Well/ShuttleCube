import { CalendarDays,Trophy } from "lucide-react";
import { StatusBadge } from "@/components/status/status-badge";

const names:Record<string,string>={experience:"体验课",camp:"集训",competition:"比赛",exclusive:"包场"};
export function EventDetail({event}:{event:{name:string;event_type:string;status:string;starts_at?:string;ends_at?:string}}){return <div><div className="rounded-md border border-slate-200 bg-slate-50 p-4"><div className="flex justify-between"><div className="flex gap-3"><span className="grid size-9 place-items-center rounded-md bg-white text-slate-600 shadow-sm"><Trophy size={16}/></span><div><h3 className="m-0 text-sm font-semibold text-slate-800">{event.name}</h3><p className="mb-0 mt-1 text-xs text-slate-500">{names[event.event_type]??event.event_type}</p></div></div><StatusBadge status={event.status}/></div></div>{event.starts_at&&<div className="mt-5 flex gap-3"><CalendarDays size={16} className="mt-0.5 text-slate-400"/><div><div className="text-[11px] text-slate-400">活动时间</div><div className="mt-1 text-sm font-medium text-slate-700">{new Date(event.starts_at).toLocaleString("zh-CN")}</div></div></div>}</div>}
