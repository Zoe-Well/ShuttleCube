<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
at specs/002-intelligent-operations/plan.md
<!-- SPECKIT END -->
## 1. 项目工作原则

1. 先理解现有代码，再进行修改。
2. 只处理当前任务需要的范围，不进行无关重构。
3. 不得擅自修改核心需求、验收标准、技术栈和公共接口。
4. 优先复用现有模块、组件和基础设施，避免重复实现。
5. 以当前代码、实际运行结果和测试结果为准，不根据文档或推测宣称功能已完成。
6. 不为了增加流程而创建无必要的文档、任务或 Skill。

## 2. 任务复杂度判断

开始任务前，先判断任务属于以下哪一类。

### 小型任务

包括：

- 局部样式或文案修改；
- 简单字段、参数或校验；
- 影响范围明确的小功能；
- 能够快速定位的简单 Bug。

处理方式：

1. 直接检查相关代码；
2. 简要说明修改方案；
3. 完成最小范围修改；
4. 执行必要验证（纯 UI 视觉调整除外）；
5. 不创建额外方案文档或执行场记。

### 复杂任务

包括：

- 涉及多个模块；
- 修改数据结构或公共接口；
- 修改 Agent、Prompt、Context、Tool、Workflow、Memory 或 RAG；
- 存在多个实现方案；
- 预计需要多轮开发或调试；
- 可能对现有功能产生较大影响。

处理方式：

1. 先检查现有代码和相关文档；
2. 在编码前形成明确方案；
3. 方案确认后再开始编码；
4. 多轮任务可按需使用 `.planning/<功能名>/` 记录执行进度。

## 3. 编码要求

1. 只修改与当前任务直接相关的代码。
2. 不擅自扩大功能范围。
3. 不同时重构无关模块。
4. 不随意更换已有框架、依赖或基础组件。
5. 公共接口发生变化时，明确说明兼容性影响。
6. 保留必要的错误处理、参数校验和日志。
7. 不通过隐藏错误、删除测试或弱化校验使任务看起来通过。
8. 修改 Agent 功能时，检查对应的 Prompt、Context、Tool、Workflow 和权限边界。
9. 高风险外部操作必须保持人工确认或现有审批机制。

## 4. 测试规则

不要求每个小变更都构建完整测试体系，但必须执行与风险匹配的验证。

### 纯 UI 视觉调整

仅修改颜色、字体、间距、尺寸、边框、阴影或排版，且不改变交互行为、业务逻辑、数据流和公共接口时：

- 不执行自动化测试、类型检查、构建检查或浏览器视觉验证；
- 完成最小范围修改后直接交付；
- 如果同时涉及组件交互、状态处理、数据行为或接口变化，则不属于纯 UI 视觉调整，仍按下述规则执行必要验证。

### 小型修改

至少完成：

- 相关功能的基本运行验证；
- 必要的类型检查、构建检查或局部测试。

### 核心或高风险修改

以下内容必须增加或执行相关自动化测试：

- 核心业务逻辑；
- 公共接口；
- 数据写入和迁移；
- 权限控制；
- 用户或项目数据隔离；
- 高风险 Agent 工具；
- 复杂状态流转；
- 已修复的重要 Bug。

### 大阶段完成后

当 MVP、核心模块、大型重构或版本开发完成时，再集中：

1. 检查当前功能状态；
2. 整理过期测试；
3. 补充单元、集成和端到端测试；
4. 补充必要的 Agent、RAG、Context 和 Memory 评测；
5. 运行项目级回归测试。
   不得把未执行的测试报告为通过。

## 5. Bug 修复强制流程 🐛

**禁止猜测根因。必须先验证，再修复。**  每次修 bug 严格按以下步骤：

1. **复现与定位** — 通过实际测试确认 bug 表现，打出关键数据（API 返回值、数据库状态、控制台日志），找到真正根因
2. **提出修复方案** — 基于测试结果写出 1-2 句修复方案，等用户确认
3. **实施修复** — 用户确认后再改代码
4. **验证修复** — 再次测试确认 bug 消失

## 6. 文档更新规则

不要求每次小改动都更新所有文档。
出现以下变化时必须更新相关文档：

- 核心功能行为变化；
- 公共接口或数据结构变化；
- Agent 架构变化；
- Prompt、Context、Tool、Workflow、Memory 或 RAG 的重要变化；
- 新增重要约束；
- 复杂功能完成；
- 复杂 Bug 暴露出长期维护注意事项。
